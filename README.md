# Redirect-extension-
A dynamic Chrome extension configuration repository. This JSON file controls which social media sites are restricted and which search engines they redirect to.

## Configuration
- `social_media`: Domains to be intercepted.
- `search_engines`: Random targets for the redirection.
